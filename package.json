
{
  "name": "playwright-api-comparator",
  "version": "1.0.0",
  "description": "Production-grade API response validation and regression framework",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf reports playwright-report test-results allure-results allure-report",
    "test": "npm run clean && npx playwright test",
    "test:serial": "npm run clean && npx playwright test --workers=1",
    "test:group": "npm run clean && npx playwright test --grep",
    "allure:generate": "npx allure generate allure-results --clean -o allure-report",
    "allure:open": "npx allure open allure-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/deep-diff": "^1.0.5",
    "@types/node": "^25.9.1",
    "allure-playwright": "^3.0.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "deep-diff": "^1.0.2"
  }
}